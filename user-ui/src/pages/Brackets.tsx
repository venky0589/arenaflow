import { Typography, Stack } from '@mui/material'

export function Brackets() {
  return (
    <Stack spacing={2}>
      <Typography variant="h5">Brackets</Typography>
      <Typography>Bracket visualization to be implemented (will render from matches/rounds).</Typography>
    </Stack>
  )
}
